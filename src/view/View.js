import styled from 'styled-components';

const Canvas = styled.div``;
const Node = styled.div``;

const View = () => {
  return (
    <Canvas>
      <Node>루트</Node>
    </Canvas>
  );
};

export default View;
